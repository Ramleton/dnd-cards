{
  "name": "dnd-cards",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && pnpm install && pnpm vite",
    "backend": "cd backend && pnpm install && pnpm start",
    "start": "concurrently \"pnpm frontend\" \"pnpm backend\""
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}