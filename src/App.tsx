import React from 'react';
import styled from 'styled-components';

const App = () => {
  return (
    <AppContainer>

    </AppContainer>
  );
};

const AppContainer = styled.div`
  display: grid;
  width: 100vw;
  height: 100vh;
`;

export default App;